import { Error } from '../../components/PopUps/Error';

export default {
  title: 'Components/PopUps/Error',
  component: Error,
};

const Template = (args) => <Error {...args} />;

export const Default = Template.bind({});
Default.args = {

};




